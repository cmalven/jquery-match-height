// Generated by CoffeeScript 1.6.3
(function() {
  var $;

  $ = jQuery;

  $.fn.extend({
    matchHeight: function(options) {
      var adjustSize, findTallest, settings,
        _this = this;
      settings = {};
      settings = $.extend(settings, options);
      adjustSize = function(evt) {
        var matchHeight;
        $(_this).css({
          "min-height": 0
        });
        if (settings.$specificTarget != null) {
          matchHeight = settings.$specificTarget.outerHeight();
        } else {
          matchHeight = findTallest();
        }
        return _this.each(function() {
          return $(this).css({
            "min-height": matchHeight
          });
        });
      };
      findTallest = function() {
        var tallest;
        tallest = 0;
        _this.each(function() {
          var thisHeight;
          thisHeight = $(this).outerHeight();
          if (thisHeight > tallest) {
            return tallest = thisHeight;
          }
        });
        return tallest;
      };
      $(window).on("resize", adjustSize);
      $(window).trigger("resize");
      return this.each(function() {});
    }
  });

}).call(this);
